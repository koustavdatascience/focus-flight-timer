/* Cloud Atlas Editorial origin configuration: change this one code to change the default departure airport. */

// Origin is intentionally empty: every focus flight begins with an explicit user choice.
export const ORIGIN_AIRPORT_CODE: string | null = null;
export const ORIGIN = null;
